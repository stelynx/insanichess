import 'package:flutter/cupertino.dart';

class ICDrawer extends StatelessWidget {
  const ICDrawer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
